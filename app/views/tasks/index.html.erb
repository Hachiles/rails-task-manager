<div class="container">
  <h1>My tasks</h1>
  <ul style="list-style-type:none;">
    <% @tasks.each do |task| %>
      <% if task.completed %>
        <input type="checkbox" id="task" checked disabled>
        <label for="task"> <%= link_to task.title.capitalize, "/tasks/#{task.id}" %> </label>
        - <%= link_to "🚮", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete #{task.title.capitalize}?" } %><br>
      <% else %>
        <input type="checkbox" id="task" disabled>
        <label for="task"> <%= link_to task.title.capitalize, "/tasks/#{task.id}" %> </label>
        - <%= link_to "🚮", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete #{task.title.capitalize}?" } %><br>
      <% end %>
    <% end %>
  </ul>
  <%= link_to "➕ Add a new task", '/tasks/new' %>
</div>